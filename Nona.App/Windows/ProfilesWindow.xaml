<Window x:Class="Nona.App.Windows.ProfilesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profiles" Height="300" Width="500" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundColor}">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    
    <TextBlock Grid.Row="0" Text="User Profiles" FontSize="18" FontWeight="Bold" 
               Foreground="{DynamicResource ForegroundColor}" Margin="0,0,0,20"/>
    
    <ListBox x:Name="List" Grid.Row="1"
             Background="Transparent"
             BorderBrush="{DynamicResource WindowBorderColor}"
             BorderThickness="1"
             Foreground="{DynamicResource ForegroundColor}"/>
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,20,0,0">
      <Button Content="Add" Margin="0,0,10,0" Click="Add_Click" Style="{DynamicResource ModernButtonStyle}"/>
      <Button Content="Select" Margin="0,0,10,0" Click="Select_Click" Style="{DynamicResource AccentButtonStyle}"/>
      <Button Content="Close" Click="Close_Click" Style="{DynamicResource ModernButtonStyle}"/>
    </StackPanel>
  </Grid>
</Window>

