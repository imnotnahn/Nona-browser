<Window x:Class="Nona.App.Windows.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="History" Height="500" Width="800" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundColor}">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <TextBlock Grid.Row="0" Text="Browsing History" FontSize="18" FontWeight="Bold" 
               Foreground="{DynamicResource ForegroundColor}" Margin="0,0,0,20"/>
    
    <TextBox x:Name="Filter" Grid.Row="1" Margin="0,0,0,15" Height="36"
             Style="{DynamicResource ModernTextBoxStyle}"/>
    
    <ListView x:Name="List" Grid.Row="2"
              Background="Transparent"
              BorderBrush="{DynamicResource WindowBorderColor}"
               BorderThickness="1"
               Foreground="{DynamicResource ForegroundColor}"
               VirtualizingPanel.IsVirtualizing="True"
               VirtualizingPanel.VirtualizationMode="Recycling"
               ScrollViewer.IsDeferredScrollingEnabled="True">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Padding="12,8" Margin="0,1" Background="Transparent">
            <Border.Style>
              <Style TargetType="Border">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Border.Style>
            <DockPanel>
              <Image Source="{Binding ThumbnailPath}" Width="64" Height="40" Margin="0,0,12,0" DockPanel.Dock="Left"/>
              <StackPanel>
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Url}" FontSize="11" Opacity="0.7"/>
              </StackPanel>
            </DockPanel>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Window>

