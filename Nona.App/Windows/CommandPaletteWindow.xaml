<Window x:Class="Nona.App.Windows.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Command Palette" Height="400" Width="700" WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True">
  <Border Background="{DynamicResource BackgroundColor}" 
          BorderBrush="{DynamicResource WindowBorderColor}" 
          BorderThickness="1" 
          CornerRadius="{DynamicResource WindowCornerRadius}"
          Effect="{DynamicResource DropShadowEffect}">
    <Grid Margin="20">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      
      <!-- Title -->
      <TextBlock Grid.Row="0" Text="Command Palette" 
                 FontSize="16" FontWeight="Bold" 
                 Foreground="{DynamicResource ForegroundColor}"
                 Margin="0,0,0,15"/>
      
      <!-- Search Box -->
      <TextBox x:Name="Query" Grid.Row="1"
               Height="40" FontSize="14"
               Background="{DynamicResource TextBoxColor}"
               Foreground="{DynamicResource ForegroundColor}"
               BorderBrush="{DynamicResource TextBoxBorderColor}"
               BorderThickness="1"
               Padding="12,8"
               Margin="0,0,0,10"/>
      
      <!-- Results List -->
      <ListBox x:Name="List" Grid.Row="2"
               Background="Transparent"
               BorderThickness="0"
               Foreground="{DynamicResource ForegroundColor}"
               MouseDoubleClick="List_MouseDoubleClick"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <Border Background="Transparent" 
                    Padding="12,8" 
                    Margin="0,1">
              <Border.Style>
                <Style TargetType="Border">
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                      <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Border.Style>
              <StackPanel>
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13"/>
                <TextBlock Text="{Binding Id}" FontSize="11" Opacity="0.7" Margin="0,2,0,0"/>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
      
      <!-- Help Text -->
      <TextBlock Grid.Row="3" 
                 Text="Type to search commands • Enter to execute • Esc to close"
                 FontSize="11" 
                 Opacity="0.7"
                 Foreground="{DynamicResource ForegroundColor}"
                 HorizontalAlignment="Center"
                 Margin="0,10,0,0"/>
    </Grid>
  </Border>
</Window>

